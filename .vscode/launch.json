{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Run Node.js Server",
            "runtimeArgs": [ "--watch" ],
            "program": "${workspaceFolder}/dist/server.js",
            "preLaunchTask": "npm: build",
        }
    ]
}
